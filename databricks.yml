bundle:
  name: model-workbench

resources:
  apps:
    app:
      name: "model-workbench"
      description: "Model Workbench — custom vision models and Foundation Model APIs in one playground"
      source_code_path: ./app
      # Uncomment to enable on behalf of user API scopes. Available scopes: sql, dashboards.genie, files.files, serving.serving-endpoints
      # user_api_scopes:
      #   - sql

targets:
  default:
    default: true
    workspace:
      host: https://<YOUR_WORKSPACE>.cloud.databricks.com
